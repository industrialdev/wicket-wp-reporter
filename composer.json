{
  "authors": [
    {
      "email": "info@wicket.io",
      "homepage": "https://wicket.io",
      "name": "Wicket Inc."
    }
  ],
  "config": {
    "platform-check": false,
    "sort-packages": true
  },
  "description": "Reports installed plugins, theme, WP core, and composer state via a secured local-only REST endpoint, for fleet-wide health monitoring.",
  "homepage": "https://github.com/industrialdev/wicket-wp-reporter",
  "keywords": [
    "wordpress",
    "fleet-health",
    "monitoring",
    "wicket"
  ],
  "type": "wordpress-plugin",
  "version": "1.1.5",
  "license": "GPL-2.0-or-later",
  "minimum-stability": "stable",
  "name": "industrialdev/wicket-wp-reporter",
  "prefer-stable": true,
  "require": {
    "php": ">=8.1"
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^3"
  },
  "scripts": {
    "check": [
      "@cs:lint"
    ],
    "cs:fix": "php-cs-fixer fix",
    "cs:format": "php-cs-fixer fix",
    "cs:lint": "php-cs-fixer fix --dry-run --diff",
    "production": "composer install && composer cs:fix && composer install --no-dev --optimize-autoloader",
    "setup-hooks": "mkdir -p .git/hooks && cp .ci/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push && echo 'Git hooks installed to .git/hooks/pre-push'",
    "version-bump": [
      "@php ./.ci/version-bump.php",
      "@cs:fix"
    ]
  },
  "scripts-descriptions": {
    "cs:format": "Run PHP CS Fixer to fix coding standards issues",
    "cs:lint": "Run PHP CS Fixer to check coding standards",
    "production": "Install production dependencies only",
    "version-bump": "Bump the plugin version in composer.json and the main plugin file"
  }
}
